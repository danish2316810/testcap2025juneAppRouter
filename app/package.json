{
  "name": "approuter",
  "dependencies": {
    "@sap/approuter": "^16.9.0"
  },
  "engines": {
    "node": "^20.0.0"
  },
  "scripts": {
    "start": "node node_modules/@sap/approuter/approuter.js",
     "build:cf": "ui5 build --all --clean-dest --dest dist"
  }
}
